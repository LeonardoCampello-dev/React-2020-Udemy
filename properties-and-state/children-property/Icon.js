import React from "react";

const styles = {
    width: "20px",
    height: "20px",
    borderRadius: "40%",
    backgroundColor: "#533772"
};

function Icon() {
    return <div style={styles} />;
}

export default Icon;
